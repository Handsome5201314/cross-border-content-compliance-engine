# 跨境内容合规引擎 · 前后端对接修复计划

> 日期：2026-09-15
> 对象：部署在 ModelScope Studio 的 Streamlit 应用（`app.py`）
> 状态：**设计文档，不动手实现，等用户确认后再执行**

---

## 一、现状诊断：前端哪些功能未与后端联通

### 根因

当前 `app.py` 是一个**演示型单文件应用**，Codex UI 重构加了品牌栏、积分胶囊、导航侧栏、隔离标签——这些**只是视觉元素，背后没有任何后端逻辑**。真正的引擎能力（core/ 下的 17 个模块）没有被接通到这些新 UI 组件上。

### 逐项梳理

| # | 前端功能（UI 已有） | 后端状态 | 未联通原因 |
|---|---|---|---|
| **F01** | 侧栏「积分 1,240 / 充值」胶囊 | ❌ **不存在**（无用户表、无积分表、无扣费逻辑） | 这是 UI 设计稿的示意数据，硬编码在 HTML 里。后端积分系统在 `ARCHITECTURE_FIXES.md` 里只是设计文档，没有实现 |
| **F02** | 侧栏「我的空间 / 文件 12 / 上传 3 / 产物 9」 | ❌ **不存在**（无文件表、无用户目录隔离） | 同上，设计稿数据。后端沙箱 `sandbox_v2.py` 已实现但只在本机测试过，未接入 Web 应用 |
| **F03** | 侧栏「最近任务（医疗出海 6/6、消费品 2/2…）」 | ⚠️ **半通**（Pipeline 有 task 状态，但没有任务列表查询 API） | Pipeline 状态机在 core/pipeline.py 里存在，但只在进程内运行，没有持久化任务列表 |
| **F04** | 侧栏「积分明细 / 充值记录 / 隐私与权限」 | ❌ **不存在** | 无用户认证体系，所有页面共用一个匿名会话 |
| **F05** | 主区「新建生成任务」表单（产品名/语种/平台/合规等级） | ✅ **已联通**（Pipeline + agents + privacy_gate 全链路可跑） | 这是目前唯一真正前后端打通的核心功能 |
| **F06** | 「演示模式」（小批次真跑 / 加载预生成全量） | ✅ **已联通**（samples/precomputed_full.json 可加载） | 可用但**没有隔离**——预生成数据所有人共享 |
| **F07** | 「生成进度（完成一条显示一条）」 | ✅ **已联通**（Pipeline 回调 → Streamlit 实时渲染） | 可用，但**没有取消功能**（UI 没有取消按钮） |
| **F08** | 合规裁决红/绿卡片 | ✅ **已联通**（agents 的四步闭环产出 verdict + reason + replacement） | 可用且是核心卖点 |
| **F09** | 业务价值看板（参数可调） | ✅ **已联通**（value_calc.py 计算器 + Streamlit number_input） | 可用，参数只在会话内生效 |
| **F10** | 导出 JSON / Markdown | ✅ **已联通**（exporter.py + download_button） | 可用，但导出文件名不含任务标识 |
| **F11** | 图像生成 | ✅ **已联通**（llm_client + image_io.py） | 可用但**计费未知**（费用显式为 unknown） |
| **F12** | 顶部「充值」按钮 | ❌ **不存在**（无支付流程） | 用户决定内测期不上充值 |
| **F13** | 侧栏「空间已隔离」标签 | ⚠️ **误导**（实际上没有做用户隔离，所有人共用一个进程） | 这是一个**危险的误导标签**——告诉用户"已隔离"但实际上没有 |

### 关键结论

**真正可用的只有 6 个功能（F05-F11）**，它们是引擎的核心链路。其余 7 个（F01-F04, F12-F13）是 UI 设计稿的**装饰性元素**，后端完全没有实现。

---

## 二、按上线优先级分级

### P0 · 上线前必须打通（否则不能对外）

| 功能 | 为什么必须 | 修复工作量 |
|---|---|---|
| **F05 生成任务全链路** | ✅ 已通，无需修复 | 0 |
| **F06 演示模式** | ✅ 已通，但需标注"预生成"避免误导 | 0.5h（加标注） |
| **F07 生成进度** | ✅ 已通，需加"取消"按钮 | 2h（Pipeline 加 cancel flag） |
| **F08 合规裁决展示** | ✅ 已通，无需修复 | 0 |
| **F13 "已隔离"标签** | **必须撤掉或改为"演示模式·数据未隔离"** | 0.5h（改一行文字） |
| **F01 积分显示** | 内测期不实现真实积分，但**不能显示假的 1,240** | 0.5h（改为"内测·积分功能开发中"或直接隐藏） |
| **F02 "我的空间"导航** | 内测期不实现文件系统，**不能显示假的文件数** | 0.5h（隐藏或标注"开发中"） |
| **F03 "最近任务"列表** | 预生成模式下可以列出已加载的任务结果 | 1h（从 session_state 读） |
| **F04 "积分明细"等** | 内测期隐藏或标注"开发中" | 0.5h |

### P1 · 上线前最好有（提升体验，但缺了也能用）

| 功能 | 说明 | 修复工作量 |
|---|---|---|
| 导出文件名带时间戳 | 方便用户区分多次导出 | 0.5h |
| Streamlit 主题优化 | 确保深色主题在 ModelScope 上正确渲染 | 1h（部署后验证+调整 CSS） |
| 预生成模式的免责声明 | 明确标注"以下是预生成结果，非实时调用" | 0.5h |
| 错误信息友好化 | 当前报错直接显示 Python traceback | 2h（加 try-catch + 自定义错误提示） |

### P2 · 可暂时延后（正式产品化时再做）

| 功能 | 说明 | 预估工作量 | 依赖 |
|---|---|---|---|
| 用户注册/登录 | 需要数据库 + 认证体系 | 3-5 天 | PostgreSQL 部署 |
| 积分扣费/充值 | 需要 ARCHITECTURE_FIXES.md 里的完整状态机 | 5-8 天 | 用户体系 + PostgreSQL |
| 文件空间隔离 | sandbox_v2 已实现但需要接入 Web 层 | 3-5 天 | 用户体系 + 文件表 |
| 任务持久化 | 跨会话保留任务历史 | 2-3 天 | PostgreSQL |
| 管理员后台 | 用户管理 / 充值 / 审计 | 3-5 天 | 用户体系 + 积分系统 |
| 在线支付 | 接支付网关 | 5-8 天 | 积分系统 |

---

## 三、修复计划（按实施顺序）

### 第一批：清理误导信息（预估 2h）

| 顺序 | 任务 | 具体改动 | 依赖 |
|---|---|---|---|
| 1 | 撤掉「空间已隔离」标签 | 改为「内测演示·无用户隔离」 | 无 |
| 2 | 积分胶囊改为静态提示 | 显示「积分功能开发中」或隐藏 | 无 |
| 3 | 「我的空间」导航隐藏 | 或标注「开发中」 | 无 |
| 4 | 「积分明细 / 充值记录 / 隐私与权限」隐藏 | 标注「开发中」 | 无 |
| 5 | 假的「最近任务」列表隐藏 | 或改为显示当前会话的实际任务 | 无 |

### 第二批：核心体验优化（预估 4h）

| 顺序 | 任务 | 具体改动 | 依赖 |
|---|---|---|---|
| 6 | 生成任务加「取消」按钮 | Pipeline 加 cancel flag，UI 加按钮 | 第一批完成 |
| 7 | 预生成模式加免责声明 | 「以下是预生成结果（2026-09-09 生成），非实时调用」 | 第一批完成 |
| 8 | 导出文件名带时间戳 | `{product}_{languages}_{timestamp}.json` | 无 |
| 9 | 错误信息友好化 | try-catch 包裹 Pipeline 调用，显示「生成失败：{原因}」 | 无 |

### 第三批：部署验证（预估 1h）

| 顺序 | 任务 | 具体改动 | 依赖 |
|---|---|---|---|
| 10 | ModelScope 部署 | push 最新代码 → ms deploy | 第一、二批完成 |
| 11 | 线上功能验证 | 用 Playwright 打开线上 Demo，逐项检查核心功能 | 10 完成 |
| 12 | 深色主题兼容性 | 确认 _theme.css 在 ModelScope 环境正确渲染 | 10 完成 |

### 总预估

| 批次 | 工作量 | 说明 |
|---|---|---|
| 第一批（清理误导） | **~2h** | 只改文字和 CSS 类，不动业务逻辑 |
| 第二批（核心体验） | **~4h** | 加取消按钮 + 免责声明 + 友好错误 |
| 第三批（部署验证） | **~1h** | push + deploy + Playwright 验证 |
| **合计** | **~7h** | 可一天内完成 |

---

## 四、修复后的验证标准

### 上线门禁（全部通过才算可上线）

| # | 检查项 | 验证方法 | 通过标准 |
|---|---|---|---|
| V01 | **无误导信息** | 打开线上 Demo，检查所有标签/文案 | 不出现「已隔离」「积分 1,240」等虚假数据 |
| V02 | **生成任务可跑** | 输入产品 → 选语种 → 点「开始生成」 | 至少完成 1 个语种的完整生成（含合规审查） |
| V03 | **合规裁决正确显示** | 生成完成后查看结果区 | 红色卡片显示违规+理由+改写，绿色卡片显示放行+理由 |
| V04 | **预生成模式可用** | 切换到「加载预生成全量」 | 能加载 `precomputed_full.json` 并显示 |
| V05 | **导出功能可用** | 点击「导出 JSON」和「导出 Markdown」 | 下载的文件可打开且内容完整 |
| V06 | **深色主题正确** | 视觉检查 | 背景 #0B0D10、无白色 Streamlit 默认元素 |
| V07 | **无 301 字样** | 页面全文搜索 | 0 处「301 医院/儿科」 |
| V08 | **回归测试通过** | `python -m unittest test_*` | 46/46 通过 |
| V09 | **线上 HTTP 200** | curl / Playwright 访问 | `https://emrdoctor-...ms.show` 返回 200 |
| V10 | **无 Python 报错** | Playwright 打开页面后检查 console | 无 uncaught exception |

### 可选增强（不影响上线但建议做）

| # | 检查项 | 验证方法 |
|---|---|---|
| V11 | 取消按钮可用 | 生成过程中点取消 → 任务停止 → 无崩溃 |
| V12 | 免责声明显示 | 预生成模式下看到标注 |
| V13 | 错误信息友好 | 断网后点生成 → 显示友好提示而非 traceback |

---

## 五、关键决策点（需要你确认）

| # | 决策 | 我的建议 | 影响 |
|---|---|---|---|
| D1 | **积分胶囊怎么处理** | 内测期显示「积分功能开发中」，不显示假数字 | 如果保留假数字 1,240，用户会质疑真实性 |
| D2 | **「我的空间」怎么处理** | 内测期隐藏整个导航组 | 如果显示假文件数 12/3/9，用户会以为有文件管理 |
| D3 | **「最近任务」怎么处理** | 改为显示当前会话的实际任务结果（从 session_state 读） | 这样有真实数据，但仅限当前会话 |
| D4 | **是否实现取消按钮** | 建议实现（Pipeline 加 flag 只需 2h） | 没有取消，长任务跑 3 分钟用户只能等 |

---

## 六、实施方式

**建议交给 Codex 执行**（加安全加固和 UI 重构的经验，它熟悉这个代码库了）。

提示词要点：
1. 只改 `app.py` 的 UI 层和 session_state 逻辑
2. 不改 `core/` 下任何模块（引擎已验证）
3. 第一批 + 第二批 + 第三批一次做完
4. 产出修改后的 `app.py` + 验证截图
5. 回归测试 46/46 必须通过

确认后我写好提示词给你。
