# 复赛作品 · 3 分钟演示指南（评审 / 用户专用）

> 作品：**跨境内容合规引擎**（参赛场景：AI 智能营销）
> 真实案例：301 儿科住院医师 AI 助手（软件）+ 智能胸卡（硬件）出海
> 评分权重：业务价值 50% + 技术 40% + 综合 10%
> 一句话：给一份产品资料，输出 6 语种独立站 / 直播内容，并在发布前拦住「不能说的话」。

本指南给两条路线，任选其一：

- **路线 A（推荐，零网络依赖）**：直接打开已生成的真实产物（HTML + JSON 审计），最稳、最快，不消耗任何 API。
- **路线 B（现场真跑）**：双击 `start_demo.bat` 启动 Web Demo，现场生成 / 加载缓存。

---

## 0. 准备（两条路线共用）

- 安装：Python 3.14（项目指定 `C:\Python314\python.exe`）+ 依赖：`C:\Python314\python.exe -m pip install -r requirements.txt`
- 凭证：程序会自动从上层目录 `E:\AI新青年\.env` 读取 `HACKATHON_API_KEY` / `HACKATHON_BASE_URL`，**无需手动配置、不出现在命令行**。
- 已生成真实产物位置：`output/delivery_site_medical_*.html`（6 语种独立站）、`output/delivery_site_medical.json`（完整审计）、`video_assets/frames/shot_*.png`（真实界面截图）。

---

## 路线 A：零 API 直接看真实产物（3 分钟，最稳）

### 步骤 1 · 看「独立站内容包」（预期：6 语种、RTL 自动排版）
- 打开文件管理器，进入 `output/` 目录。
- 双击 `delivery_site_medical_en.html`（英文独立站）——预期看到 hero / 受众 / 方案 / 能力 / 证据 / 合规 / FAQ / CTA / SEO / 声明 的完整页面。
- 双击 `delivery_site_medical_ar.html`（阿拉伯语）——**预期页面从右到左（RTL）排版**，证明非翻译器直出，是原生 RTL。
- 其余语种：`_id`（印尼）/ `_th`（泰）/ `_vi`（越南）/ `_ms`（马来）同理可打开。
- 对应真实截图证据：`video_assets/frames/shot_site_en.png`、`shot_site_ar.png`。

### 步骤 2 · 看「合规双判」（违规 vs 放行，本作品核心差异化）
- 直接用文本编辑器（或 VS Code）打开 `output/delivery_site_medical.json`。
- 跳转到 **第 1497–1519 行**（已实测核对）：
  - `s9`（约 1499 行）：`"verdict": "violation"` —— 阿语 "بنسبة 100%"（患者数据隐私 100% 保持）被判违规，理由：隐私无法作绝对承诺。
  - `s36`（约 1509–1519 行）：`"verdict": "benign"` —— "معالجة البيانات محلياً بنسبة 100%（数据 100% 本地处理不上云）" 被判放行，理由：陈述可核实技术事实。
- 同一词「100%」、同 severity high，因语境不同判出**相反结论** —— 证明这是「语境语义裁决」，不是关键词黑名单。
- 对应真实截图证据：`video_assets/frames/shot_compliance.png`。

### 步骤 3 · 看「跨境直播内容包」
- 打开 `output/delivery_live_medical_ar.html` / `delivery_live_medical_en.html` —— 八段时间轴、精确到秒、强制携带口播声明（"AI 输出为草稿，需医生审核"）。
- 消费品佐证：`output/delivery_*consumer*.html`（储能电源，跨行业复用一句佐证）。

### 步骤 4 · 下载 / 导出审计 JSON
- 直接复制 `output/delivery_site_medical.json` 即可作为完整审计证据（含每条 source_file / 生成时间 / 风险原句 / 裁决理由 / 改写结果；JSON 保留未发布风险原句供审计，HTML 仅含最终通过内容）。
- 若走路线 B 的 Web Demo，可在页面底部点「⬇️ 导出 JSON（完整调试数据+当前参数）」。

---

## 路线 B：启动 Web Demo 现场演示（需 API 可用）

### 步骤 1 · 起服务
- **双击 `start_demo.bat`**（本目录）。脚本会自动切到工作目录、启动 Streamlit。
- 浏览器自动打开（或手动访问）**http://localhost:8501**。
- 预期首页顶部：标题「🚀 跨境卖家 AI 多平台文案智造引擎 v3」+ 两条显式声明（被营销产品=全本地部署 vs 本引擎调用云端模型；隐私 / 法律提示）。

### 步骤 2 · 加载预生成全量 或 跑小批次
- 页面「③ 演示模式」有两个单选：
  - **📦 加载预生成全量**（推荐，秒开）：选它即可。若提示「未找到预生成产物」，请先离线执行一次（非现场时段，需联网）：
    ```
    C:/Python314/python.exe precompute_full.py --workers 8 --deadline 3600
    ```
    完成后回到页面重新选择即可。加载后顶部绿色提示会显示「生成时间 / 平台数 × 语种数 / 耗时 / tokens / API 估算」。
  - **🟢 小批次真跑**（现场真实调用，默认 3 平台 × 2 语种）：选它 → 点「🎬 开始生成」按钮，页面会「完成一条显示一条」。
- 若 `samples/precomputed_full.json` 已存在（本目录可能已预生成），直接选「加载预生成全量」即出结果。

### 步骤 3 · 看「独立站与跨境直播内容包」
- 加载/生成后在「文案结果」区展开任意一条（如 `tiktok × ar`）。
- 预期：标题 / 描述 / 标签 / 直播脚本 / 口播声明齐全；失败项（🟠审核未通过 / 🔴失败）会排在前面并如实展示，**不隐藏**。
- 顶部 5 个指标卡：可交付 / 审核未通过 / 生成失败 / 超时跳过 / 总 Tokens。

### 步骤 4 · 看「合规双判」
- 在「文案结果」展开一条进入页面底部的合规卡片：会显示 `merged_risk_level`、信号命中（regex_signal 待审）、语义层裁决（violation / benign）、改写建议。
- 重点看阿语条目里同一个「100%」在"隐私"语境判 violation、在"本地处理"语境判 benign —— 与路线 A 第 2 步 JSON 实证一致。

### 步骤 5 · 看阿语 RTL 页面
- 在结果筛选里只勾选语言 `ar`，展开阿语条目：正文区提供 `dir=rtl` 的安全转义阅读预览（右到左），并有纯文本复制框。

### 步骤 6 · 下载审计 JSON
- 页面「④ 业务价值看板」下方有两个下载按钮：
  - 「⬇️ 导出 JSON（完整调试数据+当前参数）」—— 完整审计。
  - 「⬇️ 导出 Markdown 发布稿（只含审核后安全版）」—— 发布稿（已剔除未通过风险原句）。

---

## 常见问题

- **端口 8501 被占用**：关闭占用程序，或 `start_demo.bat` 内加 `--server.port 8502`。
- **启动报缺 HACKATHON_API_KEY**：确认 `E:\AI新青年\.env` 存在且含该键（仅在路线 B 真跑时需要；路线 A 不需要）。
- **「加载预生成全量」提示未找到**：按路线 B 步骤 2 的离线命令先生成一次。
- **小批次真跑慢 / 失败**：属正常（依赖实时 API），可改用「加载预生成全量」或路线 A 直接看真实产物。

---

## 真实性说明（不夸大）

- 本作品的「双合规门禁 + 语境语义裁决」实证来自真实运行产物（`output/delivery_site_medical.json` 第 1497–1519 行，已实测核对）。
- 零可交付时价值看板会如实显示「无有效产出，无法估算」，不编造节省数字。
- API 成本为「估算值，非实际成本」；价值看板参数标注为「可调假设」。
