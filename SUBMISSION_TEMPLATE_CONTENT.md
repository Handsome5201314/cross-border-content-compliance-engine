# 复赛作品提交模板 · 填写内容（直接复制到 Word）

> 用法：把下面各节内容粘贴进 `复赛作品提交模板.docx` 对应位置 → 另存为 PDF
> 命名：`{团队名}_跨境内容合规引擎_复赛作品.pdf`
> ⚠️ 标记处为需要你确认/补充的信息。

---

## 一、团队信息

| 字段 | 内容 |
|---|---|
| 团队名称 | **破界AI** |
| 队长姓名 | 李帅帅 |
| 联系电话 | **13546777571** |
| 联系邮箱 | **13546777571@163.com** |
| 全体成员 | 李帅帅 / 队长（产品定义、技术实现、合规设计、内容运营，**独立完成**） |

---

## 二、参赛信息

| 字段 | 内容 |
|---|---|
| 参赛场景 | **AI智能营销** |
| 方案名称 | **跨境内容合规引擎**（Cross-Border Content Compliance Engine） |
| 一句话定义 | 为出海医疗与消费品企业解决「多语言营销内容产出慢、合规审查看不出问题」的痛点，把单条 6 语种文案的产出从 6.5 小时压到 3 分钟，并在发布前拦住违规表述。 |

---

## 三、业务价值与市场分析

### 2.1 目标用户与业务痛点

**目标用户（两类，共用一套引擎）**

1. **医疗健康出海企业**——以《301 儿科住院医师助手》为真实标的：一套**全本地部署**的儿科临床 AI 助手。软件侧已跑通（Hermes Agent 按本院模板生成文书 + 医师自生长知识库，Obsidian 兼容 markdown + git + 每日加密备份）；硬件侧为**佩戴式智能胸卡（在研）**——墨水屏状态 + 自动录音 + 拍照 OCR + 待办提醒，患儿资料 100% 本机处理、任何数据不上云 API。能力清单见 `samples/product_medical_appliance.json`（7 项软件 `implemented` / 6 项硬件 `planned`），实机证据见 `住院医师智能胸卡/` 下的产品照 `jacket-v1-*.jpg`、实机视频 `eink4.mp4`、固件与集成证据 `固件备份/20260911-022921/integration-capability-evidence.json`。正计划进入东南亚与中东市场。
2. **跨行业通用性佐证**——以户外便携储能电源为对照品类验证：换一份产品 JSON、不写一行代码即可切换口吻与合规规则，证明本引擎是「内容生产线」而非医疗模板（详见 2.4）。

**痛点一：多语言内容产出成本高、周期长（真实调研数据）**

我们调研了公开报价与实时挂牌价（完整来源清单见附录）：

| 环节 | 市场报价 |
|---|---|
| 医疗专业翻译（TEP） | $0.15–0.30 / 源词 |
| 通用翻译 | $0.10–0.16 / 源词 |
| 社媒文案撰写 | $30–75 / 帖 |
| 落地页文案 | $150–500 / 页 |
| 医疗类溢价 | 在上述基础上 **+20%~40%** |
| Fiverr 实时挂牌（实抓） | $5 / $10–15 / $25–40 / Vetted Pro $100–140 |

**结论**：一条产品文案翻译成 6 个语种（en/ar/id/th/vi/ms），人工约 **165 美元、6.5 小时**。

**痛点二：合规审查是人工盲区，且后果严重**

- 阿语、印尼语、泰语、越南语、马来语的绝对化用语，**中文母语者无法识别**；
- 各国准入与数据主权要求差异极大（印尼 BPOM / 沙特 SFDA / 阿联酋 MOHAP / 欧盟 MDR / 美国 FDA；GDPR / 印尼 PDPA / 中东数据本地化）；
- 医疗类的表述风险更高：一句「疗效承诺」或「隐私 100% 保障」就可能构成违规宣传。

**痛点三：阿语等 RTL 语言排版容易出错**，靠人工粘贴常出现方向错乱。

### 2.2 应用场景与使用流程

**场景**：出海企业拿到一份产品资料（能力清单 + 红线约束），需要在几天内产出面向多个市场的独立站文案、FAQ、SEO 描述与跨境直播脚本。

**完整链路（三步，无需写代码）**

1. **输入**：一份产品 JSON——包含产品能力清单、技术栈、红线约束、目标市场（本方案真实使用了 13 项能力，其中硬件 6 项明确标注为「规划中」）。
2. **生成**：引擎按目标语种并行生成「独立站内容包」或「跨境直播脚本」，每个语种经过 **生成 → 合规审查 → 改写 → 复审** 四步。
3. **交付**：输出 JSON 交付包（含完整审计留痕）+ 各语种 HTML 页面（阿语自动 RTL 排布）。

命令行入口：`site_cli.py --product <产品.json> --langs en,ar,id,th,vi,ms`
或 `live_cli.py` 生成直播脚本。

### 2.3 商业价值与预期效果（**全部为真实运行数据**）

**成本与效率**

| 项目 | 人工 | 本引擎（真实实测） |
|---|---|---|
| 单条 6 语种文案 | 约 6.5 小时 / 约 $165 | **约 3 分钟 / API 成本约 ¥0.19** |
| 合规检查 | 依赖人工，小语种基本不可行 | 每句过检，命中即改写并复审 |
| 阿语 RTL 排版 | 人工易错 | 自动 RTL 输出 |

**真实运行记录（可核验）**

- 医疗独立站 6 语种：**6/6 全部交付**，14 次模型调用，140,866 tokens，API 成本估算 **¥0.1859**，生成耗时约 **170–202 秒**；
- 消费品独立站：2/2 交付；医疗直播脚本：3/3；消费直播脚本：3/3；
- 累计真实调用 **17 批 87 次 / 833,004 tokens**（含失败与重试，全部保留记录）。

**合规能力实证（最核心的差异化）**

引擎在同一批真实输出中，对**同一个词「100%」、同一条正则信号（severity: high）**，因语境不同判出了相反结论：

| 语种 | 原句 | 裁决 | 理由 |
|---|---|---|---|
| 阿语 | 患者数据隐私 **100%** 保持于本地网络 | ❌ **违规** | 隐私无法作绝对承诺，存在语义风险 → 改写为「通过本地处理保护」 |
| 印尼语 | 数据处理 **100%** 在本地服务器 | ✅ **放行** | 陈述可核实的技术事实，不涉及安全/疗效承诺 |

**这证明它是语境语义裁决，而非关键词黑名单**——关键词层只负责捞出可疑句，最终由语义层判断"这句话到底在承诺什么"。纯正则方案在此处必然「要么全拦、要么全放」，两种都是错的。

同类真实案例：印尼语直播稿「Ini **sempurna** untuk kebutuhan sehari-hari」（完美）被判绝对化，改为「**cocok**」（合适）并通过复审。

**必须说明的边界（不夸大）**：这三分钟是起点不是终点，仍需约 0.3 小时人工复核（母语者校对 + 医生审核临床表述）。**我们不替代人，替代的是那 6.5 小时里的重复劳动。**

### 2.4 市场前景与落地可行性

**市场规模**：中国医疗健康与消费品出海均处于高速增长期，东南亚（印尼/泰国/越南/马来）与中东（沙特/阿联酋）是当前增量最快的两个区域，多语言内容与合规审查是共性刚需。

**商业模式**

1. **SaaS 订阅**：按语种数 / 内容量分档计费；
2. **交付式服务**：为客户一次性建立本地化内容库（本项目已验证「内容包 + HTML 交付」的形态）；
3. **与硬件/产品捆绑**：医疗侧配合《住院医师助手 + 智能胸卡》整体出海。其差异化卖点之一正是「数据主权：全本地推理、数据不出院」，与各国数据本地化监管方向一致，无需跨境传输评估。

**可规模化复制性（已验证）**

引擎的核心是**产品 JSON 作输入、合规规则集分行业配置**。实测：把医疗产品换成消费品，**不写一行新代码**，仅替换一份 JSON，输出口吻即从「Local-First Pediatric Clinical Documentation Assistant」切换为消费品文案，合规规则也自动从「临床表述」切换到「价格承诺与虚假客户」。

→ 说明它不是医疗专用模板，而是**通用的内容生产线**。

本作品的头号真实案例即上述《301 儿科住院医师助手 + 智能胸卡》（软件已实现、硬件在研、数据不出院）；消费者储能电源仅作「换 JSON 不换代码」的通用性佐证，不并列展开。

**落地路径**：先以医疗出海为差异化切口（合规门槛高 = 竞争少），再横向复制到消费品品类。

---

## 四、产品功能与使用说明

### 产品简介

**跨境内容合规引擎**是一套面向出海企业的多语言营销内容生产线。用户输入一份产品资料，引擎输出多个语种的独立站文案与跨境直播脚本，并在发布前完成合规审查与自动改写。它解决的是「小语种内容产出慢」与「小语种合规审查没人看得懂」这两个叠加痛点，目标用户是医疗健康与消费品出海企业。

（真实案例：本作品即服务于团队自研的《301 儿科住院医师 AI 助手 + 智能胸卡》出海——软件侧 Hermes Agent 与医师自生长知识库已在临床场景跑通，硬件侧智能胸卡在研，全链路本地部署、数据不出院；引擎为其生成面向 ID/TH/VN/MY/SA/AE/RU 七国市场的多语种合规营销内容。消费者品类（户外便携储能电源）仅作为「换 JSON 不换代码」的跨行业佐证。）

### 核心功能清单

1. **多语言内容生成**——单次产出 6 语种（en/ar/id/th/vi/ms）独立站内容包，覆盖 hero / 受众 / 方案 / 能力 / 证据 / 合规 / FAQ / CTA / SEO / 声明。
2. **双合规门禁**——营销文案合规（绝对化用语、疗效承诺、免责声明）+ 产品准入与数据主权（BPOM / SFDA / MOHAP / MDR / FDA、GDPR / 印尼 PDPA / 中东数据本地化）。
3. **语境语义裁决**——正则层只做召回，语义层做判定，同一个「100%」在不同语境可分别判为违规与放行（见 2.3 实证）。
4. **自动改写 + 复审**——命中风险后给出改写建议并强制复审，未复审不得进入交付。
5. **跨境直播脚本生成**——八段时间轴、精确到秒、互动主题、停顿重音，并**强制携带口播声明**（AI 输出为草稿，需医生审核后使用），该声明删除即判交付失败。
6. **多语种 HTML + RTL 输出**——阿语自动从右到左排版，非翻译器直出。
7. **完整审计留痕**——每条保留 source_file / 生成时间 / 风险原句 / 裁决理由 / 改写结果；JSON 保留未发布风险原句供审计，HTML 仅含最终通过内容。

### 使用说明

**环境**：Windows / Python 3.14；依赖见 `requirements.txt`（openai、pyyaml、jsonschema、streamlit）

**运行独立站内容包**
```
python site_cli.py --product samples/product_medical_appliance.json --langs en,ar,id,th,vi,ms
```

**运行跨境直播脚本**
```
python live_cli.py --product samples/product_medical_appliance.json --langs en,ar,id
```

**可视化界面**
```
streamlit run app.py
```
在页面顶部展开「独立站与跨境直播内容包」，可查看交付快照、风险原句/原因/改法对照表，并下载审计记录。

**消费品对照**：把 `--product` 换成 `samples/product_consumer_demo.json` 即可，无需改代码。

---

## 五、技术架构及调用模型说明

### 系统架构图

```
                    ┌─────────────────────────┐
   产品资料 JSON ───▶ │  产品门禁 & 隐私白名单    │
 （能力/红线/目标市场） │  (privacy_gate)         │
                    └───────────┬─────────────┘
                                ▼
              ┌─────────────────────────────────────┐
              │        多语种并行任务调度             │
              │      (package_cli · 串行/并行)       │
              └───┬─────────────┬─────────────┬─────┘
                  ▼             ▼             ▼
          ┌─────────────┐ ┌───────────┐ ┌─────────────┐
          │ 独立站 Agent │ │ 直播 Agent │ │ 内容包 Agent │
          │ site_agent  │ │ live_agent│ │package_agent│
          └──────┬──────┘ └─────┬─────┘ └──────┬──────┘
                 └──────────────┼──────────────┘
                                ▼
              ┌─────────────────────────────────────┐
              │  四步闭环 / 每个语种：                │
              │  ① 生成 ─▶ ② 合规审查(跨厂商交叉)     │
              │       ─▶ ③ 改写 ─▶ ④ 复审            │
              └───────────┬─────────────────────────┘
                          ▼
            ┌──────────────────────────────┐
            │  交付层                        │
            │  · JSON 交付包（含审计留痕）    │
            │  · 多语种 HTML（阿语 RTL）      │
            │  · usage / counts / 失败不伪装  │
            └──────────────────────────────┘
```

**关键设计决策**

- **字段合同「净化 > 判死」**：早期实现在某个语种的 bullets 混入空值时，整个语种被判 SchemaError 而全灭（实测 tiktok 因此 0 交付）。改为**先净化再校验**（把非法值转成「非空字符串数组」），把「判死」降级为「净化 + 告警」，交付率从 0/4 提升到 3/4。**可用性优先于严格性。**
- **合规门禁 fail-closed**：不确定就拦，宁可漏交付也不放违规内容出去。
- **长度超限做软告警不做 block**：阿语标题 43 字符超过建议值 40 时，仅告警不阻断——**长度不是红线，医疗合规才是**。

### 调用的阿里云百炼模型

| 模型标识 | 用途 | 调用方式 |
|---|---|---|
| `qwen3.7-plus` | 内容生成（独立站 / 直播脚本 / 多语种改写） | 通过 Token Plan 网关，OpenAI 兼容接口 |
| `deepseek-v4-flash` | 合规审查（跨厂商交叉审查，降低同源盲区） | 同上 |

> 合规审查刻意**换厂商**执行：生成用通义系、审查用 DeepSeek 系，避免「自己写自己审」的同源盲区。
> 早期合规审查用 glm-5.2，实测在审查长文案时 90s 超时导致整条判失败，改用 deepseek-v4-flash（实测 1.4s）后稳定，同时保留跨厂商交叉审查的价值。

### 技术组件说明

| 层 | 技术 |
|---|---|
| 语言 / 运行时 | Python 3.14 |
| LLM 接入 | `openai` SDK（OpenAI 兼容），网关 `token-plan.cn-beijing.maas.aliyuncs.com` |
| 配置驱动 | `pyyaml`（`config/site_blocks.yaml`、`live_script.yaml`、`package_copy.yaml`、`compliance.yaml`、`languages.yaml`、`platforms.yaml`） |
| 结构校验 | `jsonschema` |
| 界面 | Streamlit |
| 数据处理 | 纯文件式 JSON 流水线，无外部数据库依赖（便于私有化部署） |
| Agent 组织 | 多 Agent 分工（site / live / package）+ 单 Agent 内四步闭环，非简单 Prompt 串联 |

---

## 六、项目开发及阶段成果说明

**开发过程**

1. 初赛完成多平台社媒文案引擎（6 平台 × 7 语种差异化生成，GitHub 仓库 `cross-border-ai-marketing-copy`）。
2. 复赛收窄场景至「医疗出海」，并新增两个深度模块：**独立站内容包**与**跨境直播脚本**。
3. 经独立评审（Codex 对抗式评审）后重做，修复 4 个 P0 级问题。

**已完成的真实成果**

| 交付物 | 状态 |
|---|---|
| 医疗独立站内容包 | **6/6 语种交付**（en/ar/id/th/vi/ms） |
| 消费品独立站内容包 | 2/2 交付（en/ar） |
| 医疗跨境直播脚本 | 3/3 交付（en/ar/id） |
| 消费跨境直播脚本 | 3/3 交付（en/ar/id） |
| 离线回归测试 | 15 个新增 unittest 全部通过 |
| 交付包一致性校验 | 四包全部通过（6/6、2/2、3/3、3/3），含篡改拒绝验证 |
| 真实调用记录 | 17 批 87 次 / 833,004 tokens，失败记录全部保留 |

**遇到的挑战与解决方案**

1. **交付率突然归零**——某语种因字段类型错误被判死，下游全部失败。→ 改为「净化 > 判死」，把严格校验降级为净化 + 告警。
2. **合规审查超时**——审查模型在长文案上 90s 超时。→ 换用更快模型，同时保留跨厂商交叉审查的设计价值。
3. **长度限制误伤**——阿语标题超字符建议值导致整条被拦。→ 区分「硬红线」与「软建议」，长度改为告警。
4. **历史稿质量缺陷**——发现中文混入与品牌名误改后，增加门禁并**重新真实生成**，同时**撤回了 12 份不合格的历史 HTML**（原始 JSON 保留供审计）。

**后续迭代计划**

1. 接入真实留资后端与网站部署，打通「生成 → 上线」链路；
2. 扩充语种与行业合规规则集（俄语区、拉美）；
3. 准入/认证/数据主权从 `pending_verification` 升级为人工核实工作流。

**诚实声明的局限**

- 模型审核不等于法律核验，准入/认证/数据主权统一标记为「待人工核实」；
- 真实生成仍可能失败（结构不完整、被隐私门禁拦截、复审不通过），失败记录全部保留，**不伪装为成功**；
- 尚未搭建真实留资后端、未部署上线，本次交付的是内容包。

---

## 七、提交物清单（勾选）

- ☑ 项目开发及阶段成果说明（本模板第六部分）
- ☑ 技术架构及调用模型的使用说明（本模板第五部分）
- ☑ 产品功能与使用说明（本模板第四部分）
- ☑ 可运行的产品 Demo / 核心功能原型：`start_demo.bat` 一键启动 Streamlit；`DEMO_GUIDE.md` 三分钟演示路径；`samples/precomputed_full.json`（42 条真实产物，18 交付 / 6 审核未过 / 18 失败，失败项如实保留、不伪装）
- ☑ 产品演示视频（已出片）：`final/破界AI_跨境内容合规引擎_复赛演示.mp4`（150 秒 / 1920×1080 / 中文字幕烧录 / 无配音）；公开链接待上传（B站登录态失效，需手动上传后回填）
- ☑ **GitCode 代码仓库地址**：`https://gitcode.com/Handsome5201314/cross-border-content-compliance-engine`
  （公开仓库，无需登录即可浏览代码与交付文件）
- ☑ **GitHub 镜像仓库**（备用链接）：`https://github.com/Handsome5201314/cross-border-content-compliance-engine`
- ☑ 测试账号或体验地址：**无需账号**（仓库公开可直接访问；Demo 本地一键启动 `start_demo.bat`）

---

## 附录 A：数据来源（业务价值可核验）

成本基线含 8 个可核查来源，含 Fiverr 实时挂牌价实抓。完整清单见项目内 `baseline_research.md`。

## 附录 B：已知问题与待办（主动披露）

- 3 项 SEO 字符数仍为软告警：消费站英文 description 166/160、医疗站印尼语 description 163/160、医疗站越南语 title 63/60。遵守「长度不 block」口径，未假称满足限制，投放前需精简并复审。
- 消费品独立站其他语种、直播 th/vi/ms 未做真实 API 验收。
- 已做 HTML 安全与 RTL 方向验证，未做人工浏览器逐页视觉验收。
