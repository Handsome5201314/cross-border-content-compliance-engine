# 跨境内容合规引擎 · 产品化升级 PRD（V2）

> 文档类型：下游需求文档（供架构师 / 工程师 / QA 使用）
> 产品负责人：许清楚（Xu，产品经理）
> 版本：V2 · 初稿
> 关联项目：黑客松复赛作品「破界 AI · 跨境内容合规引擎」，部署于 ModelScope 创空间（Streamlit 单容器，公网 `https://emrdoctor-cross-border-content-compliance-engine.ms.show`）
> 上游依据：`INTEGRATION_FIX_PLAN.md`（13 项功能诊断）、主理人确认的需求范围与已锁定技术决策

---

## 0. 文档说明与资料校验

本 PRD 为**需求层**文档，描述"要做什么 / 验收标准是什么"，不规定表结构、代码结构、状态机实现（属架构师职责，见任务 #3）。

**资料校验发现（请主理人/架构师关注）：**
- 必读文档 `ARCHITECTURE_FIXES.md`（积分系统状态机设计）在当前仓库中**不存在**。积分状态机设计由架构任务（#3）补出，本 PRD 仅描述积分的**功能需求与业务规则**。
- `core/sandbox_v2.py`（文件沙箱隔离，原称"已实现但仅本机测过"）在当前仓库中**不存在**。P2(d) 文件空间隔离需从零实现，不能"直接接入已有模块"。
- 其余依据 `INTEGRATION_FIX_PLAN.md` 与 `app.py` 现状均已核对，下文现状描述以 `app.py` 实测为准。

---

## 1. 产品目标

**一句话定位：** 在保留"AI 多语种营销内容生成 + 四步合规闭环"核心能力的前提下，把当前"演示型单文件应用"升级为一个**有真实用户体系、真实积分、真实文件隔离与真实任务历史的可用产品**，并先消除一切误导用户的假数据。

### 成功标准（可衡量，验收可写成测试）

| # | 成功标准 | 衡量方式 |
|---|---|---|
| S1 | **零误导信息**：上线后页面全文不出现"空间已隔离""积分 1,240""文件 12/上传 3/产物 9""仅你可见"等虚假或误导文案 | 自动扫描 + 人工逐页核对，命中数 = 0 |
| S2 | **全链路可跑**：新注册用户用初始积分可完成 ≥1 次多语种实时生成，积分按语种数正确扣减并被明细记录 | 端到端用例通过；积分余额变化 = 扣减额；明细流水条数 = 1 |
| S3 | **100% 真实数据**：我的空间文件数、最近任务、积分余额、积分明细均来自数据库，非硬编码 | 数据库直查与页面展示一致；无硬编码常量 |
| S4 | **管理员可运营**：管理员可查看用户列表并禁用用户、手动充值并产生充值流水、查看任务与积分审计流水 | 管理员端 3 类操作各至少有 1 条成功记录 |
| S5 | **任务跨会话持久化**：用户退出后重登，"最近任务"与积分余额与退出前一致 | 重登后数据一致性校验通过 |

---

## 2. 用户角色与权限矩阵

角色定义：
- **访客（Guest）**：未登录用户。
- **注册用户（User）**：已注册并登录的普通用户。
- **管理员（Admin）**：具备运营后台权限的用户。

| 功能 / 能力 | 访客 | 注册用户 | 管理员 | 说明 |
|---|:---:|:---:|:---:|---|
| 体验演示模式（预生成数据） | ✅ | ✅ | ✅ | 免登录即可，见 R21 |
| 价值看板 | ✅ | ✅ | ✅ | 免登录即可，见 R21 |
| 用户注册 | ✅（可注册） | —（已登录） | —（已登录） | 见 R10 |
| 用户登录 / 登出 | ✅ | ✅ | ✅ | 见 R11 |
| 实时生成任务 | ❌ | ✅ | ✅ | 登录后解锁，见 R13 |
| 生成任务取消 | ❌ | ✅ | ✅ | 见 R06 |
| 积分查看 / 积分明细流水 | ❌ | ✅（本人） | ✅（本人） | 见 R14 |
| 我的空间（输入/产物按用户隔离） | ❌ | ✅（本人目录） | ✅（本人目录） | 见 R17 |
| 任务持久化 / "最近任务"真实列表 | ❌（仅当前会话） | ✅ | ✅ | 见 R18 |
| 导出结果（JSON / Markdown） | ⚠️（仅演示结果） | ✅ | ✅ | 文件名带可识别信息，见 R08 |
| 充值（被管理员加积分） | ❌ | ✅（被动接收） | ✅（被动接收） | 见 R16 |
| 管理员后台 · 用户管理（列表 / 禁用） | ❌ | ❌ | ✅ | 见 R19 |
| 管理员后台 · 手动充值 | ❌ | ❌ | ✅ | 见 R16 |
| 管理员后台 · 审计（任务流水 / 积分流水） | ❌ | ❌ | ✅ | 见 R20 |

> 矩阵中"✅（本人）"表示仅能访问自己的数据；管理员后台操作为跨用户运营能力。

---

## 3. 用户故事（按角色分组）

每条用户故事含 **验收标准（AC）**，AC 需可达到"能写成测试用例"的程度。

### 3.1 访客（Guest）

**US-G1 · 免登录体验合规能力**
作为访客，我希望不登录就能体验演示模式与价值看板，以便快速判断产品价值。
- AC1：未登录直接进入应用，可见"演示模式"入口并能加载预生成全量产物。
- AC2：未登录可见价值看板并能调整参数查看估算。
- AC3：未登录时，"实时生成""我的空间""积分"等需登录功能不可触发，点击/提交时引导登录而非报错或静默失败。

**US-G2 · 不被误导**
作为访客，我希望看到的所有状态/数字都是真实的，以免被虚假信息误导。
- AC1：页面不出现"空间已隔离""仅你可见""积分 1,240""文件 12/上传 3/产物 9"等文案（对应 S1）。
- AC2：若某功能在内测期未实装，其入口显示明确"开发中"标注或隐藏，不展示假数据。

### 3.2 注册用户（User）

**US-U1 · 注册账号**
作为新用户，我想用用户名 + 密码注册账号，以便使用实时生成等高级能力。
- AC1：注册表单校验用户名唯一性；重复用户名提示"该用户名已被注册"。
- AC2：密码以 bcrypt 加密存储，数据库内不可见明文（安全验收）。
- AC3：注册成功自动登录，并赠送初始积分（额度见待确认 Q1），余额立即可见且真实。

**US-U2 · 登录与登出**
作为用户，我想登录后使用完整功能，并能在结束时登出。
- AC1：用户名/密码错误时提示"用户名或密码错误"，不暴露具体哪一项错。
- AC2：登录后侧栏积分胶囊、我的空间、最近任务均显示本人真实数据。
- AC3：登出后回到访客态，演示模式与看板仍可用，但实时生成等被拦截。

**US-U3 · 实时生成并扣积分**
作为用户，我想提交多语种生成任务，系统按语种数扣积分。
- AC1：提交时按所选语种数量 × 单价预扣（单价见 Q2）；扣减后余额实时刷新。
- AC2：生成可中途取消（见 R06），取消后已扣除积分的处理规则见 Q4。
- AC3：生成过程中若余额不足，提交即被拦截并友好提示"积分不足，请联系管理员充值"（见 R15）。

**US-U4 · 查看积分明细**
作为用户，我想看到积分余额与每一笔收支明细。
- AC1：积分明细含：初始赠送、每次生成扣费、每次管理员充值，含时间、类型、变更额、变动后余额。
- AC2：明细与余额长期一致，跨会话不丢失（见 R18）。

**US-U5 · 我的独立空间**
作为用户，我希望我上传的资料和生成产物只在我自己的目录里，他人不可见。
- AC1：上传文件存于本人隔离目录；生成产物存于本人隔离目录。
- AC2：A 用户无法通过任何界面/接口访问 B 用户的文件或产物。
- AC3：我的空间的"文件数/上传数/产物数"来自真实统计，非硬编码。

**US-U6 · 任务历史可回溯**
作为用户，我希望"最近任务"显示我真实的任务历史，且跨会话保留。
- AC1："最近任务"列表每条含：产品名、语种数、平台数、完成时间、成功/拦截概览。
- AC2：退出重登后列表与退出前一致（对应 S5）。

**US-U7 · 友好地导出**
作为用户，我想导出结果且文件名能让我区分不同任务。
- AC1：导出 JSON / Markdown 文件名含产品名 + 语种数 + 时间戳（见 R08）。
- AC2：导出失败或被拦截时给出友好提示，不抛 traceback。

### 3.3 管理员（Admin）

**US-A1 · 用户管理**
作为管理员，我想查看所有用户并禁用违规账号。
- AC1：用户列表含用户名、注册时间、状态（正常/禁用）、当前积分。
- AC2：可禁用用户；禁用后该用户无法登录或生成（具体态见 Q6），操作被记录。

**US-A2 · 手动充值**
作为管理员，内测期无支付网关，我想直接给指定用户加积分。
- AC1：输入用户名 + 充值额 + 备注，提交后该用户余额增加。
- AC2：每次充值生成充值流水（见 R16），可在审计中追溯。

**US-A3 · 审计**
作为管理员，我想审计任务流水与积分流水，以便运营排查。
- AC1：任务流水含用户、任务、时间、语种/平台、消耗积分、结果概览。
- AC2：积分流水含全部用户的所有积分变动（赠送/扣费/充值）。
- AC3：流水可按用户、时间筛选查看。

---

## 4. 需求池（P0 / P1 / P2）

编号规则：Rxx 唯一编号；优先级 P0（上线前必须）/ P1（体验优化）/ P2（正式产品化）。

### 4.1 P0 · 清理误导（上线门禁，否则不能对外）

| 编号 | 需求 | 描述 | 验收标准 | 依赖 |
|---|---|---|---|---|
| R01 | 改写"空间已隔离"标签 | 撤除主区 `空间已隔离` 标签，改为明确"当前为共享演示环境，未做用户隔离"的文案 | 页面无"空间已隔离"字样；新文案准确描述无隔离事实 | 无 |
| R02 | 清理假积分展示 | 顶部/侧栏 `积分 1,240` 与 `注册赠送 666·按 token 倍率扣费` 提示移除；内测未实装期显示"积分功能开发中"，实装后显示真实余额 | 无硬编码假积分数字；提示文案与实际计费规则一致（按语种数，非 token 倍率） | R13（实装后接真实值） |
| R03 | 清理"我的空间"假文件数 | 侧栏 `我的文件 12 / 我上传的资料 3 / 生成产物 9` 移除；实装后端（R17）显示真实统计，未实装期隐藏或标"开发中" | 无硬编码文件数常量 | R17 |
| R04 | 清理假"最近任务"与空功能入口 | 侧栏假"最近任务"（医疗出海 6/6 等）、"积分明细/充值记录/隐私与权限"移除；实装后接真实数据，未实装期隐藏或标"开发中" | 无虚构任务项；空功能不展示假链接 | R14、R18、R19、R20 |
| R05 | 改写空间"仅你可见"误导文案 | 侧栏 `你的空间仅你可见，AI 只在你自己的目录内读写` 在未隔离期移除/改写，与 R01/R17 状态一致 | 文案与系统实际隔离能力一致，不夸大 | R17 |

### 4.2 P1 · 体验优化（提升体验，缺了也能用）

| 编号 | 需求 | 描述 | 验收标准 | 依赖 |
|---|---|---|---|---|
| R06 | 生成任务取消按钮 | Pipeline 增加 cancel 标志，UI 在生成中显示"取消"按钮 | 生成中点取消 → 任务停止 → 无崩溃/无残留进程；页面回到可重新提交态 | 无（仅 UI + Pipeline flag，不改 core 引擎逻辑） |
| R07 | 预生成模式免责声明 | 预生成/演示模式明确标注"以下内容为离线预生成结果，非实时调用" | 演示模式下可见醒目免责声明，含预生成时间（已有 `generated_at` 可复用） | 无 |
| R08 | 导出文件名带可识别信息 | 导出文件名由 `copy_engine_result_{ts}` 改为含产品名 + 语种数 + 时间戳 | 文件名形如 `{product}_{langN}lang_{ts}.json/.md`；多次导出可区分 | 无 |
| R09 | 错误信息友好化 | 捕获 Pipeline / IO / 网络异常，向用户展示中文友好提示，不暴露 Python traceback | 任意异常路径（断网、文件损坏、超时）均显示友好文案；控制台可记日志但页面不显 traceback | 无 |

### 4.3 P2 · 正式产品化功能

| 编号 | 需求 | 描述 | 验收标准 | 依赖 |
|---|---|---|---|---|
| R10 | 用户注册 | 用户名 + 密码注册；密码 bcrypt 加密存储；用户名唯一 | 见 US-U1；密码明文不在库；重复用户名报错 | 无 |
| R11 | 用户登录 / 登出 | 基于会话的登录；登录态用于解锁高级功能 | 见 US-U2；错误凭证统一提示；登出回到访客态 | R10 |
| R12 | 注册赠送初始积分 | 新用户注册即获初始积分（额度见 Q1） | 注册后余额 = 初始额度；产生一条"赠送"明细 | R10、R14 |
| R13 | 实时生成按语种数扣积分 | 实时生成任务按所选语种数量扣积分（单价见 Q2）；图像生成是否计费见 Q3 | 提交预扣 = 语种数 × 单价；余额实时更新；产生"扣费"明细；扣费与生成结果一致 | R11、R14 |
| R14 | 积分明细流水 | 记录每笔积分变动（赠送/扣费/充值），含时间、类型、变更额、变动后余额 | 见 US-U4；明细与余额长期一致、跨会话保留 | R12、R13、R16 |
| R15 | 余额不足友好拦截 | 实时生成提交时若余额不足，拦截并友好提示 | 提交即拦截不进入生成；提示"积分不足，请联系管理员充值"；不扣费 | R13、R14 |
| R16 | 管理员手动充值 | 内测期无支付网关，管理员后台手动给指定用户加积分，记录充值流水 | 见 US-A2；充值后余额增加；充值流水可追溯 | R11（管理权限）、R14 |
| R17 | 登录用户独立文件空间 | 登录用户每人独立空间：上传输入文件、生成产物均按用户隔离存储（架构对接隔离层；**当前 sandbox_v2.py 缺失，需新建**） | 见 US-U5；A 不可访问 B 数据；空间统计真实 | R11 |
| R18 | 任务持久化 | 任务历史跨会话保留；"最近任务"显示真实数据 | 见 US-U6；重登后列表一致 | R11、R14 |
| R19 | 管理员后台 · 用户管理 | 用户列表（用户名/注册时间/状态/积分）；可禁用用户 | 见 US-A1；禁用操作被记录 | R11（管理权限） |
| R20 | 管理员后台 · 审计 | 任务流水 + 积分流水，可按用户/时间筛选 | 见 US-A3；流水完整可查 | R14、R18 |
| R21 | 访问策略落地 | 免登录可体验演示模式 + 价值看板；登录后解锁实时生成、积分、我的空间 | 见 US-G1 / 矩阵；未登录触发需登录功能时引导登录而非报错 | R10、R11 |

> 说明：原需求书称"P2 五项正式产品化功能"，实际列项为 a–f 共 6 项（注册登录/积分/充值/隔离/持久化/管理后台），本池已全数编入 R10–R20（积分拆分为 R12–R14、管理后台拆分为 R19–R20）。

---

## 5. 关键业务流程

### 5.1 注册 / 登录流程
```
[访客] --注册(用户名+密码)--> [后端 bcrypt 存密 + 建用户] --赠送初始积分(R12)--> [自动登录]
[访客] --登录(用户名+密码)--> [校验 bcrypt] --成功--> [建立会话, 解锁高级功能]
                                     --失败--> [提示"用户名或密码错误"]
[已登录] --登出--> [清除会话] --> [访客态]
```
验收：注册即登录且余额=初始额；错误凭证不暴露具体错误项；登出后高级功能被拦。

### 5.2 扣积分流程与余额不足处理
```
[已登录用户] --提交实时生成(选 N 个语种)--> [校验余额 ≥ N×单价?]
   ├─ 否 --> [拦截, 提示"积分不足, 请联系管理员充值"(R15), 不扣费, 不进入生成]
   └─ 是 --> [预扣 N×单价, 余额实时刷新, 记"扣费"明细(R14)]
            --> [执行生成(Pipeline)]
                 ├─ 完成 --> [任务落库(R18), 结果可导出]
                 ├─ 取消(R06) --> [停止, 已扣积分处理见 Q4]
                 └─ 失败 --> [任务记失败, 积分处理见 Q4]
```
验收：扣减额 = 语种数 × 单价；余额与明细一致；不足时绝不进入生成。

### 5.3 管理员充值流程
```
[管理员] --后台选择用户 + 输入充值额 + 备注--> [校验用户存在]
   --> [该用户余额 += 充值额] --> [记"充值"明细(R14)] --> [记充值流水(R16)]
```
验收：充值后用户余额增加且可在审计流水查到；备注必填或留痕。

### 5.4 文件隔离规则
```
[已登录用户 U] --上传资料--> [存 /users/U/inputs/]
[已登录用户 U] --生成产物--> [存 /users/U/outputs/]
[任意用户 V≠U] --访问 U 的资源--> [拒绝(无权)]
```
验收：每个登录用户拥有独立目录；跨用户访问被拒；侧栏统计来自真实目录计数（R03/R05/R17）。

### 5.5 任务生命周期
```
创建(提交) → 扣费(R13/R15) → 生成中(可取消 R06) → 完成/拦截/失败
   → 落库(R18) → 出现在"最近任务"(真实数据) → 可导出(R08) → 可审计(R20)
跨会话：用户退出 → 数据保留于库 → 重登后"最近任务"/余额一致(S5)
```

---

## 6. 待确认问题（仅列真正的产品歧义 + 资料偏离）

| # | 问题 | 影响范围 | 建议（待主理人拍板） |
|---|---|---|---|
| Q1 | 注册初始积分额度是多少？ | R12、S2 | 建议内测期给一个可跑通多次生成的额度（如 100–200） |
| Q2 | 实时生成每语种扣费单价是多少？ | R13、S2 | 建议按语种数定单价（如每语种 10 积分），与现有假提示"按 token 倍率"脱钩 |
| Q3 | 图像生成（F11）是否计入积分消耗？当前计费未知 | R13 | 建议内测期图像生成不额外计费或单列规则，需明确 |
| Q4 | 单条重跑 / 失败项重跑 / 用户主动取消，是否二次扣费或退还？ | R13、R06 | 建议：取消/失败不扣或全额退还；重跑按新语种数重新计费 |
| Q5 | 管理员账号如何初始化？（系统种子账号 / 首个注册用户自动为管理员） | R16、R19、R20 | 建议预留种子管理员账号，避免权限自举风险 |
| Q6 | 用户被禁用后状态？（禁止登录 / 仅禁止生成） | R19 | 建议"禁用=禁止登录"，最清晰可测 |
| Q7（偏离） | `ARCHITECTURE_FIXES.md` 与 `core/sandbox_v2.py` 在仓库中不存在（见第 0 节） | R14 状态机设计、R17 文件隔离 | 积分状态机由架构任务(#3)补出；R17 文件隔离需从零实现，不能"接入已有模块" |

> 若上述 Q1–Q6 主理人无特别指示，工程师可按"建议"列实现，PRD 验收以实际配置值为准。

---

## 7. 明确不做（范围边界）

- 不做在线支付 / 支付网关接入（充值仅管理员手动，见 R16）。
- 不修改 `core/` 下 17 个引擎模块的逻辑（仅接入 Web 层，必要时加 cancel flag 属 Pipeline 调度层，不改引擎算法）。
- 不做多端 / App / 第三方账号（OAuth）登录，仅用户名 + 密码。
- 不做用户间协作 / 团队空间（仅个人独立空间，见 R17）。

---

## 8. 下游交接提示

- **架构师（任务 #3）**：需补 `ARCHITECTURE_FIXES.md` 积分状态机设计；设计 SQLite 单文件库于 `/mnt/workspace/data/`，DAO 抽象层预留切 PostgreSQL；文件隔离层需新建（sandbox_v2 缺失）。
- **工程师（任务 #2）**：P0 仅改 `app.py` 文案/CSS 与少量调度；P2 涉及认证、积分、隔离、持久化、后台，严格在 Web 层实现，不碰 core 引擎。
- **QA（任务 #4）**：S1–S5 为上线门禁；P0 验收可脚本扫描关键词；P2 验收按第 3 节用户故事 AC 写用例。
